# 19. 설정 및 프로필

---

## 19a. 설정

### 기본 정보

| 항목 | 내용 |
|------|------|
| URL | `/settings` |
| 파일 | `src/app/settings/page.tsx` |
| 레이아웃 | AdminLayout |
| 인증 | 필요 |

---

### 화면 구조

```
AdminLayout
└── div.space-y-6
    ├── PageHeader
    └── 설정 Card 그리드 (2열)
        ├── 프로필 설정 Card
        ├── 보안 설정 Card
        ├── 알림 설정 Card
        └── 시스템 설정 Card
```

---

### PageHeader

| 항목 | 내용 |
|------|------|
| title | 설정 |
| description | 시스템 설정을 관리합니다 |
| action | 없음 |

---

### 프로필 설정 Card

```
Card
├── CardHeader
│   ├── CardTitle: "프로필 설정"
│   └── CardDescription: "기본 계정 정보를 변경합니다"
└── CardContent
    ├── 닉네임 필드 (Label + Input)
    ├── 이메일 필드 (Label + Input, type="email")
    └── Button "저장" (우측 정렬)
```

---

### 보안 설정 Card

```
Card
├── CardHeader
│   ├── CardTitle: "보안 설정"
│   └── CardDescription: "비밀번호와 보안을 관리합니다"
└── CardContent
    ├── 현재 비밀번호 (Label + Input, type="password")
    ├── 새 비밀번호 (Label + Input, type="password")
    ├── 새 비밀번호 확인 (Label + Input, type="password")
    └── Button "비밀번호 변경" (우측 정렬)
```

---

### 알림 설정 Card

```
Card
├── CardHeader
│   ├── CardTitle: "알림 설정"
│   └── CardDescription: "알림 수신 방법을 설정합니다"
└── CardContent (space-y-4)
    ├── Switch 항목 1: 이메일 알림
    │   └── div.flex.items-center.justify-between
    │       ├── Label "이메일 알림"
    │       └── Switch (checked=emailNotification)
    ├── Switch 항목 2: 푸시 알림
    ├── Switch 항목 3: 게임 이벤트 알림
    └── Switch 항목 4: 시스템 알림
```

**Switch 알림 항목 목록**:

| 항목 | 기본값 | 설명 |
|------|--------|------|
| 이메일 알림 | true | 중요 이벤트를 이메일로 수신 |
| 푸시 알림 | false | 브라우저 푸시 알림 |
| 게임 이벤트 알림 | true | 게임 시작/종료 알림 |
| 시스템 알림 | true | 서버 오류 및 경고 알림 |

---

### 시스템 설정 Card

```
Card
├── CardHeader
│   ├── CardTitle: "시스템 설정"
│   └── CardDescription: "시스템 환경을 설정합니다"
└── CardContent (space-y-4)
    ├── 언어 설정 (Label + Select)
    │   └── 옵션: 한국어 / English
    ├── 시간대 설정 (Label + Select)
    │   └── 옵션: Asia/Seoul / UTC / ...
    └── Button "저장" (우측 정렬)
```

---

### 상태 관리 (settings)

```typescript
const [emailNotification, setEmailNotification] = useState(true);
const [pushNotification, setPushNotification] = useState(false);
const [gameEventNotification, setGameEventNotification] = useState(true);
const [systemNotification, setSystemNotification] = useState(true);
```

---

## 19b. 프로필

### 기본 정보

| 항목 | 내용 |
|------|------|
| URL | `/profile` |
| 파일 | `src/app/profile/page.tsx` |
| 레이아웃 | AdminLayout |
| 인증 | 필요 |

---

### 화면 구조

```
AdminLayout
└── div.space-y-6
    ├── PageHeader
    └── grid.gap-6.md:grid-cols-2
        ├── 프로필 정보 Card (좌)
        └── 계정 정보 Card (우)
```

---

### PageHeader

| 항목 | 내용 |
|------|------|
| title | 프로필 |
| description | 프로필 정보를 관리합니다 |
| action | 없음 |

---

### 프로필 정보 Card

```
Card
├── CardHeader
│   ├── CardTitle: "프로필 정보"
│   └── CardDescription: "계정 기본 정보를 확인하고 수정할 수 있습니다"
└── CardContent.space-y-6
    ├── 프로필 이미지 영역
    │   ├── Avatar (h-24 w-24)
    │   │   ├── AvatarImage (src=profileImageUrl || adminUser.profileImageUrl)
    │   │   └── AvatarFallback (이니셜, text-2xl)
    │   ├── 편집 모드: 카메라 버튼 (label for="profile-image")
    │   │   └── Input type="file" accept="image/*" (hidden)
    │   └── 이름 + 이메일 텍스트
    ├── 닉네임 필드
    │   ├── 보기 모드: div (adminUser.nickname || '-')
    │   └── 편집 모드: Input
    ├── 이메일 필드
    │   ├── 보기 모드: div (adminUser.email || '-')
    │   └── 편집 모드: Input type="email"
    ├── 역할 필드 (읽기 전용)
    │   └── div (adminUser.role || 'USER')
    └── 버튼 영역 (justify-end)
        ├── 보기 모드: Button "수정" → setIsEditing(true)
        └── 편집 모드:
            ├── Button "취소" (variant="outline") → setIsEditing(false)
            └── Button "저장" (Save 아이콘) → handleSave()
```

---

### 프로필 이미지 이니셜 생성 로직

```typescript
const initials = (adminUser?.nickname || adminUser?.email || 'A')
  .split(' ')
  .map((n) => n[0])
  .join('')
  .toUpperCase() || 'AD';
```

---

### 계정 정보 Card

```
Card
├── CardHeader
│   ├── CardTitle: "계정 정보"
│   └── CardDescription: "계정 관련 추가 정보"
└── CardContent.space-y-4
    ├── 사용자 ID
    │   ├── Label: "사용자 ID"
    │   └── div (font-mono, text-sm): adminUser.id || '-'
    └── 역할
        ├── Label: "역할"
        └── div (text-sm): adminUser.role || 'USER'
```

- 계정 정보 Card는 읽기 전용 (수정 불가)

---

### 상태 관리 (profile)

```typescript
const [isEditing, setIsEditing] = useState(false);
const [profileImage, setProfileImage] = useState<File | null>(null);
const [profileImageUrl, setProfileImageUrl] = useState<string | null>(null);
const [formData, setFormData] = useState({
  nickname: '',
  email: '',
});

// adminUser 로드 시 폼 초기화
useEffect(() => {
  if (adminUser) {
    setFormData({
      nickname: adminUser.nickname || '',
      email: adminUser.email || '',
    });
    setProfileImageUrl(adminUser.profileImageUrl || null);
  }
}, [adminUser]);
```

---

### 이미지 업로드 처리

```typescript
const handleImageChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (file) {
    setProfileImage(file);
    // FileReader로 미리보기 URL 생성
    const reader = new FileReader();
    reader.onloadend = () => {
      setProfileImageUrl(reader.result as string);
    };
    reader.readAsDataURL(file);
  }
};
```

---

### 로딩 상태

```
isLoading = true
→ div.flex.h-screen.items-center.justify-center
    └── div.text-muted-foreground "로딩 중..."
```

---

### 데이터 연동

| Hook | 역할 |
|------|------|
| `useCurrentUser()` | 현재 로그인 사용자 정보 조회 |

반환 타입: `AdminInfo` (`id`, `email`, `nickname`, `role`, `profileImageUrl`)

---

### 저장 처리 (handleSave)

```typescript
// 현재: TODO 상태 (API 미연동)
// 성공 시: Toast "프로필이 성공적으로 업데이트되었습니다" + setIsEditing(false)
// 실패 시: Toast(destructive) "프로필 업데이트에 실패했습니다"
```

---

### 토스트 메시지

| 액션 | 성공 | 실패 |
|------|------|------|
| 프로필 저장 | "프로필 업데이트 - 프로필이 성공적으로 업데이트되었습니다" | "오류 - 프로필 업데이트에 실패했습니다" |
